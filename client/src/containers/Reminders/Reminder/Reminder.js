// Dependency imports
import React from 'react';

// Asset imports

// Component
const reminder = (props) => {
    return(
        <div>
            {props.text}
        </div>
    )
};

export default reminder;